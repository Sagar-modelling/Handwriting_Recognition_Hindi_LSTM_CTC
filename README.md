In this notebook, we'll go through the steps to train a CRNN (CNN+RNN) model for hindi handwriting recognition. The model will be trained using the CTC(Connectionist Temporal Classification) loss.

Still Working on it !!
